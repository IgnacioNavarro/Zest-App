<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Zest" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/Primary_Color</item>
        <item name="colorPrimaryDark">@color/Background</item>
        <item name="colorAccent">@color/Red_error</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="AppTheme" parent="Theme.Zest">
        <item name="android:timePickerDialogTheme">@style/myTimePickerStyle</item>
    </style>

    <style name="myTimePickerStyle" parent="@android:style/Widget.Material.Light.TimePicker">
        <item name="android:numbersTextColor">@color/Background</item>
        <item name="android:numbersBackgroundColor">@color/white</item>
        <item name="android:numbersSelectorColor">@color/Primary_Color</item>
        <item name="android:headerBackground">@color/grey_lighter</item>
    </style>
</resources>